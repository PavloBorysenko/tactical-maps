<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="/">
            <i class="fas fa-map me-2"></i>Tactical Maps
        </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link px-3 {% if app.request.get('_route') == 'admin_dashboard' %}active{% endif %}" href="{{ path('admin_dashboard') }}">
                        <i class="fas fa-tachometer-alt me-1"></i>Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 {% if app.request.get('_route') == 'map_index' or app.request.get('_route') starts with 'map_' %}active{% endif %}" href="{{ path('map_index') }}">
                        <i class="fas fa-map me-1"></i>Maps
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 {% if app.request.get('_route') == 'side_index' or app.request.get('_route') starts with 'side_' %}active{% endif %}" href="{{ path('side_index') }}">
                        <i class="fas fa-users me-1"></i>Sides
                    </a>
                </li>
            </ul>
            
            <ul class="navbar-nav ms-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-user-shield me-1"></i>{{ app.user.email|slice(0, 15) }}{% if app.user.email|length > 15 %}...{% endif %}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><h6 class="dropdown-header"><i class="fas fa-crown me-1"></i>Administrator</h6></li>
                        <li><span class="dropdown-item-text small">{{ app.user.email }}</span></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">
                            <i class="fas fa-sign-out-alt me-2"></i>Logout
                        </a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav> 